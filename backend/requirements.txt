# Core API + kernel + persistence (installable everywhere)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
python-multipart>=0.0.9
httpx>=0.27
structlog>=24.1

# Agent layer (Groq / OpenRouter via LiteLLM) + workflow orchestration
litellm>=1.44
langgraph>=0.2
langchain-core>=0.3


# Document ingestion
pymupdf>=1.24

# pgvector support (Postgres deployments)
pgvector>=0.3

# Temporal workflows (background worker)
temporalio>=1.6
